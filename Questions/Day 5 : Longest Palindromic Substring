class Solution {
    public boolean isPalindrome(int x) {
        int results = 0;
        int o =x;
        if(x < 0){
            return false;
        }
        while(x!=0){
            int dig = x%10;
            x/=10;
            results = results*10+dig;

        }
        return o == results;
    }
}
